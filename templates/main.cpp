#include <iostream>
#include <vector>
#include <unordered_map>
#include <string>

#define LOG(x) cout << x << endl;
#define IN(x) cin >> x;
#define LLI long long int

// PROBLEM :

using namespace std;

int main()
{
	return 0;
}
