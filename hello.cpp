#include <iostream>

int main()
{
  std::cout << "Hello, this is my competive programming sandbox" << std::endl;
  std::cout << "ENV, clang-cl (installed by vs 2022) and vscode" << std::endl;
  std::cout << "each problem is solved in a single file for portability, hence there won't be any inclues except the standard library" << std::endl;
  std::cout << "if you are able to run/debug this simple program you all set to code" << std::endl;

  return 0;
}